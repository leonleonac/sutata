# 数据分析回归软件项目大纲（PRD + 技术实施版）

## 1. 项目愿景与目标
构建一款“低门槛、可解释、可复现”的回归分析工具，面向不会编程但需要严肃统计分析的用户。

### 1.1 核心价值主张
- **像 Excel 一样上手**：左侧网格视图直接看数据、选变量、做轻量清洗。
- **像统计软件一样可靠**：右侧即时给出规范回归结果（系数、显著性、拟合优度、诊断提示）。
- **像协作工具一样可复现**：模型参数、变量选择与导出结果可追踪。

### 1.2 北极星指标（North Star）
> 用户在 5 分钟内，从上传数据到得到“可解释的首个回归结果”。

---

## 2. 用户画像与关键使用场景

### 2.1 用户画像
1. **本科/研究生用户**：用于课程作业与论文预分析。
2. **研究助理（RA）**：用于变量筛选、模型初步检验。
3. **业务分析师**：在运营/市场/风控中快速验证变量影响方向。

### 2.2 关键场景（Job To Be Done）
- 我有一份 Excel/CSV/DTA 数据，希望不用写代码就能完成 OLS 回归。
- 我想快速测试多组解释变量组合，并比较结果稳定性。
- 我需要把结果导出为可直接放到汇报或论文附录中的表格。

---

## 3. 产品范围（In Scope / Out of Scope）

### 3.1 In Scope（MVP）
- 上传并解析 `.xlsx/.xls/.csv/.dta`。
- 左侧类 Excel 表格浏览（排序/筛选/基础缺失值处理）。
- 变量角色选择：
  - 被解释变量（Y）：单选
  - 解释变量（X）：多选
  - 控制变量（C）：多选
- OLS 回归：支持常数项、稳健标准误（HC1）。
- 右侧结果展示：系数、SE、t 值、P 值、显著性星号、R²、Adj.R²、N、F-stat。
- 结果导出：CSV + 复制为 Markdown 表格。

### 3.2 Out of Scope（MVP 不做）
- 因果推断高级方法（DID、RDD、PSM、IV-2SLS）。
- 团队协作权限体系（多租户、RBAC）。
- 复杂 ETL（跨表 join、数据仓库连接器）。

---

## 4. 核心交互与页面结构

## 4.1 双栏布局
- **左栏（数据与变量）**
  - 顶部：上传按钮 + 数据集信息（行数、列数、缺失比例）
  - 中部：可滚动表格（虚拟滚动）
  - 底部：变量选择器（Y/X/C）
- **右栏（模型与结果）**
  - 顶部：模型参数（是否含常数、稳健 SE 开关）
  - 中部：回归结果主表
  - 底部：诊断提示区（共线性、样本损失、异常值提醒）

### 4.2 关键交互流程
1. 上传文件后自动预览前 200 行。
2. 用户在变量面板设定 Y、X、C。
3. 点击“运行回归”或开启“自动运行”。
4. 右侧展示结果；若失败则返回可读错误（含建议修复操作）。
5. 用户可导出表格或保存当前模型快照。

---

## 5. 功能模块设计

### 5.1 数据导入模块
- 支持编码识别（CSV UTF-8/GBK 尝试）。
- 首次导入进行字段类型推断：numeric/category/datetime/string。
- 返回导入报告：
  - 总行数、总列数
  - 每列缺失率
  - 可用于回归的数值列清单

### 5.2 数据网格模块（左侧）
- 基础能力：排序、筛选、冻结列、列宽拖拽。
- 数据处理（MVP）：
  - 删除缺失行（按当前模型变量）
  - 数值列标准化（z-score，可选）
  - 对数变换（log(x+1)，仅非负列）
- 变量选择防呆：
  - Y 仅允许数值列
  - X/C 可选数值列；分类列提示“先做哑变量转换（后续版本自动化）”

### 5.3 回归引擎模块（右侧）
- 计算库：`statsmodels.api.OLS`
- 输入：
  - `y_col: string`
  - `x_cols: string[]`
  - `c_cols: string[]`
  - `fit_options: { intercept: boolean, robust_se: 'none' | 'HC1' }`
- 输出：
  - 参数表（coef、std_err、t、p、ci）
  - 模型统计量（R²、Adj.R²、F、Prob(F)、N）
  - 警告信息（奇异矩阵、完全共线性、样本过小）

### 5.4 导出与复现模块
- 导出 CSV（含模型统计量 + 参数表）。
- 一键复制 Markdown 回归表。
- 保存模型快照（变量配置 + 参数 + 时间戳）。

---

## 6. 非功能需求（NFR）

### 6.1 性能
- 10 万行以内数据：表格滚动流畅（虚拟列表）。
- 单次 OLS（<=30 个变量）计算时长目标：< 2 秒。

### 6.2 稳定性
- 导入失败与计算失败均需可读错误码和文案。
- 回归接口可重复调用，保证幂等（同输入同输出）。

### 6.3 安全与隐私
- 默认本地会话存储，不主动上传第三方。
- 服务端日志脱敏（不记录完整数据，仅记录元信息）。

---

## 7. 技术方案

### 7.1 架构
- 前端：React + TypeScript + Ant Design + AG Grid。
- 后端：FastAPI。
- 计算：pandas + statsmodels。
- 存储：SQLite（MVP）用于保存会话快照与模型历史。

### 7.2 API 设计（MVP）
1. `POST /api/datasets/upload`
   - 入参：文件
   - 出参：`dataset_id`、schema、preview
2. `POST /api/models/run`
   - 入参：`dataset_id + y/x/c + fit_options`
   - 出参：`summary + coefficients + warnings`
3. `GET /api/models/{id}/export.csv`
   - 返回：可下载结果文件

### 7.3 数据处理流程
1. 文件上传
2. 解析并类型推断
3. 前端展示预览并选择变量
4. 后端按变量切片、丢弃缺失、构建设计矩阵
5. 执行 OLS 并返回结构化结果

---

## 8. 里程碑计划（6 周 MVP）

- **第 1 周**：产品原型、交互走查、数据样例集准备。
- **第 2 周**：上传解析 + 表格预览 + 字段类型推断。
- **第 3 周**：变量选择器 + 模型参数面板。
- **第 4 周**：OLS 引擎 + 结果表渲染。
- **第 5 周**：导出功能 + 错误处理 + 警告系统。
- **第 6 周**：性能优化、测试、上线文档与演示脚本。

---

## 9. 验收标准（Definition of Done）

### 9.1 功能验收
- 可导入三类文件（Excel/CSV/DTA），并成功展示预览。
- 能完成 Y/X/C 选择并运行 OLS。
- 结果包含 coef、SE、p 值、R²、N。
- 导出 CSV 成功，内容与界面一致。

### 9.2 质量验收
- 至少 10 个真实样例文件回归成功率 >= 95%。
- 关键接口单测覆盖率 >= 80%（计算与格式化逻辑）。
- 用户可在 5 分钟内完成首个回归（可用性走查）。

---

## 10. 风险与应对
- **大文件卡顿**：采用列裁剪 + 虚拟滚动 + 分块读取。
- **数据脏导致回归失败**：增强导入报告与变量可用性提示。
- **统计结果误读**：增加指标解释悬浮提示与“结果解读说明”。
- **多重共线性**：添加 VIF（后续版本）与自动警告。

---

## 11. 后续迭代路线（V1.1 ~ V2.0）
- V1.1：Logit/Probit、哑变量自动编码。
- V1.2：固定效应（entity/time FE）、聚类稳健标准误。
- V1.3：残差图、QQ 图、杠杆值诊断。
- V2.0：自动报告生成（Word/PDF）+ AI 解释助手（结果摘要与建议）。
